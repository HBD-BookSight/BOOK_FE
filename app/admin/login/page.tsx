import LoginForm from "./components/loginForm/LoginForm";

const LoginPage = () => {
  return (
    <div className="relative flex size-full min-h-[100vh] flex-col items-center justify-center">
      <LoginForm />
    </div>
  );
};

export default LoginPage;
