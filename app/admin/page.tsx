import LoginForm from "@/components/loginForm/LoginForm";

const page = () => {
  return (
    <div className="relative flex size-full min-h-[100vh] min-w-[100vw] flex-col items-center justify-center">
      <LoginForm />
    </div>
  );
};

export default page;
